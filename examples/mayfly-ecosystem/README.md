# Mayfly ecosystem example bundle

An opt-in composition bundle that installs the shared user kit and activates
five runnable examples through five ordinary Cordis rows.

```sh
dsh plugin --profile mayfly-examples add @mayfly-example/ecosystem
```

Install Mayfly in the same profile first. This validation bundle is excluded
from Mayfly's default product composition and release set.
